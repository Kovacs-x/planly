Realtime path: migration 015's `planly_broadcast_household_task_change` trigger fires AFTER UPDATE on `planly_tasks`. Because the assignee RPC updates the existing Household task row without changing `household_id`, the same private `household:<uuid>` broadcast used for creator changes is emitted. The client treats broadcast as an invalidation signal and reconciles through the existing RLS-protected cloud read path.