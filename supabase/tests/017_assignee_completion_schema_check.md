Schema verification for migration 017: `planly_tasks` fields used are `owner_id`, `client_id`, `deleted_at`, `visibility`, `household_id`, `assignee_id`, `completed`, `data`, and `client_updated_at`. These are established by migrations 007, 013 and 016. No generic `updated_at` field is referenced. Migration 015 already broadcasts every `planly_tasks` UPDATE to the private Household topic when `household_id` is present.