Failure behaviour: if the RPC rejects because assignment/membership changed, the client does not mutate local completion state and shows a retry message. If the network request fails, the task remains in its prior state. This slice intentionally does not queue assignee completion offline because the assignee is not the task owner and the existing owner mutation queue must not be reused for foreign-owned rows.