Race handling: the RPC selects the task `FOR UPDATE` before validating current assignee and membership. Reassignment or unsharing that commits first causes the completion call to validate the new state and fail; completion that obtains the lock first completes under the then-current valid assignment and subsequent creator changes reconcile normally. The RPC does not trust the client-side assignee pill for authorization.